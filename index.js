const { writeMutations } = require("./mutations");
const { mapWritableState } = require("./state");

// const { writeMutations } = require("./mutations");
// const { mapWritableState } = require("./state");

module.exports = {
  writeMutations,
  mapWritableState,
};
